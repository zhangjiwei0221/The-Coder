# 更新日志

## v0.35 - 2026-05-13

- 调整抽牌/弃牌循环：用过的牌进入对应弃牌堆，只有当对应抽牌堆为空时，才将弃牌堆洗回抽牌堆。
- 保留当前指令牌堆与参数牌堆双牌堆结构，分别处理各自的抽牌堆、弃牌堆和重洗。
- 第一层普通怪接入 `BUG`、`Error`、`回环`、`岔路`。
- 第一层精英池接入 `DDL`，并保留 `催单员`、`待办残片`。
- 接入敌人透明 PNG 素材渲染，支持 `img/enemies/*.png` 作为战斗敌人立绘。
- 易伤改为次数制：接下来 X 次受到攻击伤害 +50%，触发一次消耗一层。
- 优化指令卡：`双击` 改为参数型卡，造成 X 点伤害两次；`蓄力` 改为下回合攻击伤害 +50%，按四舍五入结算。
- 优化牌堆信息显示：指令抽牌堆/弃牌堆与参数抽牌堆/弃牌堆分开展示，并在手牌区显示下回合指令/参数抽牌数量。
- 修复 `红字审判` 等敌人随机弃牌时只删除手牌、不刷新界面的问题；现在被弃掉的牌会进入对应弃牌堆，并同步清空选中状态。
- 优化商店体验：每个商店节点生成固定库存，购买后显示售罄，不再刷新成新商品；删牌服务改为指定删除牌库中的一张牌。
- 新增牌库查看弹窗，可在地图、奖励、事件、休息、宝箱和商店中查看当前构筑、卡牌数量、类型与描述。
- 扩充事件池至 10 个，并用办公数字世界主题重写事件文案与选项。
- 修复精英/Boss 奖励插件卡的一处残留乱码和潜在报错。
- 更新怪物配置表与完整游戏策划案中的抽牌规则描述。
- 第一层参赛 Demo 收束：击败 `红字审判` 后进入 `Demo 完成 / 未完待续` 结算，不再继续进入第二层。
- 新增第一场 `BUG` 战斗的新手引导，提示查看敌方代码、拖入指令、填入参数并运行。
- 为 `DDL` 与 `红字审判` 补充统一终端风格 SVG 立绘，并接入战斗显示。
- 为 `红字审判` 增加战前叙事提示，强化第一层 Boss 仪式感。
- 清理战斗界面残留的 `//` 注释式标题文案。
- 修复敌人立绘被 DOM 层与战斗特效层重复绘制的问题，现在战斗中只显示一套敌方素材。
- 移除战斗特效层在敌人立绘下方额外绘制的白色敌人名，避免与底部 UI 名称重复。
- 精简比赛版入口：首界面只保留 `开始调试`，点击后使用默认起始牌组直接进入地图，不再显示选角色界面。
- 精简地图界面：移除顶部区域名与列阶段标签，保留路径节点和玩家状态。
- 回调地图视觉实验，保持地图为清晰路径节点；优化战斗左侧资源栏，增加 HP/参数进度条、护盾模块和牌堆卡片化显示。
- 取消选角后的可见角色信息：移除战斗 HUD 中的角色身份卡，首回合循环抽牌日志改为系统提示。
- 优化左侧牌堆栏可读性：将压缩的 `抽/弃/+N` 改为 `抽牌堆`、`弃牌堆`、`下回合抽` 三项明确展示。
- 将卡牌 `消耗` 统一改为 `本战移出`：只在当前战斗离开牌堆，战斗结束后不影响整局牌库。
- 优化指令设计：`护盾` 改为更有定位的 `壁垒`，调整治疗、双击、反弹、吸血、压制等强力牌说明；收束奖励池中的内部判断变体。
- 优化状态显示：空状态显示 `无异常`，状态标签改为更完整的中文说明。
- 重构休息区：改为 `休整`、`整理牌库`、`预编译` 三选一，分别对应回血、删牌精简和下一战首回合额外抽牌。
- 修复快速拖拽时的禁止光标残留：拖拽中的卡牌现在由窗口层统一维持移动状态，拖过空白或不可放置区域后再回到合法槽位，不会继续显示禁止图标；释放到空白处会安静取消。
- 补齐 `DDL` 与 `红字审判` 的敌人立绘素材：使用色键图抠成透明 PNG，并替换战斗中的敌人配置路径。
- 重绘并替换第一层普通怪 `BUG`、`Error`、`回环`、`岔路` 的立绘素材，统一到新的数字办公室怪物风格，并更新透明 PNG 资源。
